'use client';

import { useEffect } from 'react';

/**
 * Visual Editing overlay component for Sanity.
 * Renders click-to-edit overlays in preview mode.
 * Uses @sanity/presentation when available.
 */
export default function VisualEditing() {
  useEffect(() => {
    // Visual editing is handled by @sanity/presentation when configured
    // This component serves as the mount point
  }, []);
  return null;
}
